document.addEventListener('DOMContentLoaded', (event) => {
    var video = document.getElementById('bg-video');
    video.playbackRate = 0.8; // Ajusta este valor para cambiar la velocidad de reproducción
});
